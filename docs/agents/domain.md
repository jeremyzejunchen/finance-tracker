# Domain docs

This is a single-context repository.

Before design or implementation work, read the root `CONTEXT.md` and every relevant decision in `docs/adr/`.

Use terms as defined in `CONTEXT.md`. If a proposed change conflicts with an ADR, surface the conflict instead of silently overriding the decision.
