# 项目总览与下一步

## 目标

维护个人财务流水的导入、清洗、审计与界面功能，同时保护真实财务数据。

## 当前状态

- 已从 `D:\finance-tracker` 迁入 Workspace。
- 已复制代码、文档、Git 历史、原始账务数据和银行流水。
- 已排除缓存、虚拟环境和临时目录。
- 迁移前已存在的 `AGENTS.md` 未提交修改已原样保留在源与目标中。

## 下一步

1. 在项目目录运行 `pwsh -NoProfile -File .\scripts\doctor.ps1`，确认本机环境。
2. 如需重建环境，按项目 AGENTS 规则显式运行 bootstrap 脚本。
3. 开始新任务前阅读本项目 AGENTS、全局控制台和相关文档。

## 最近验证

- 迁移清单：1,362 个文件，49.25 MiB。
- 关键文件：README、AGENTS、项目配置和账务数据均存在。
- Git：源与目标提交一致，仓库完整性检查通过。
